// Build-time constants injected by vite.config.js `define`. The only reader
// is src/lib/build-info.ts — see that module for the rationale.
declare const __MYTODO_BUILD__: import("$lib/core/build-info").BuildInfo;
